{
  "name": "agentkit",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:api": "npm run dev -w backend",
    "dev:ui": "npm run dev -w frontend",
    "test": "npm test -w backend"
  }
}
